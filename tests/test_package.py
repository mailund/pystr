"""
Test that we can load the package.

We don't do anything else, we just load it. I can't imagine
what could go wrong, but if I don't do it, I will find out
too late.
"""

import pystr  # noqa: F401 pylint: disable=unused-import
